var minhaVar  = 'minha variavel';

function minhaFunc(x, y){
    return x+y;
}

let num = 2;
const PI = 3.14;

var numeros = [1,2,3];
numeros.map(valor => valor * 2); // ES 2015

class Matematica{
    soma (x,y){
        return x+y;
    }
}
var n1: String = 'asaoapo'; // tipágem de variáveis no TypeScript
var n2: number = 1;
